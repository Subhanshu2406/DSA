const int INF = 1e9;
vector<vector<pair<int, int>>> adj;

void dijkstra(int s, vector<int>& d, vector<int>& p){
    vector<int> dist(n);

    pq;
    d[s] = 0;
    while(!pq.empty()){
        
    }
}